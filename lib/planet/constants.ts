import * as THREE from "three";

export const EARTH_RADIUS = 1;

export const CLOUD_RADIUS = 1.025;

export const ATMOSPHERE_RADIUS = 1.05;

export const EARTH_ROTATION_SPEED = 0.0001;

export const CLOUD_ROTATION_SPEED = 0.00012;

export const DEFAULT_SEGMENTS = 128;

export const DEFAULT_RINGS = 64;

export const SUN_DISTANCE = 15;

export const DEFAULT_UP = new THREE.Vector3(0, 1, 0);